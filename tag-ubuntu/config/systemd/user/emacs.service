[Unit]
Description=Emacs Daemon

[Service]
Type=notify
Type=notify
ExecStart=/home/linuxbrew/.linuxbrew/bin/emacs --fg=daemon
ExecStop=/home/linuxbrew/.linuxbrew/bin/emacsclient --eval "(kill-emacs)"
Restart=on-failure

[Install]
WantedBy=default.target
